# Core matcher + tests
rapidfuzz>=3.0
pytest>=8.0

# Data download / pipeline
requests>=2.31

# Web API (phase 2)
fastapi>=0.110
uvicorn>=0.27
httpx>=0.27   # FastAPI TestClient 需要它

# AI 消歧(阶段 6)
anthropic>=1.0

# 数据库(阶段 3)
sqlalchemy>=2.0
"psycopg[binary]">=3.1
